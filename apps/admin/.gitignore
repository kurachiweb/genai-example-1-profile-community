# Next.js / テスト成果物
.next
out
coverage
next-env.d.ts
*.tsbuildinfo

# OpenNext(Cloudflare)ビルド成果物・wrangler devのローカル状態
.open-next
.wrangler

# コンテナ内でのみ使えるライブラリ用ディレクトリ(ルート直下の lib/ のみ対象。先頭の / が無いと
# src/lib 等も誤って無視されるため注意、client/.gitignore と同じ書式に揃える)
/lib
